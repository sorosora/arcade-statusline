[package]
name = "arcade-statusline"
version = "0.1.0"
edition = "2024"
description = "Arcade-style statusline for Claude Code"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
strip = true
lto = true
opt-level = "s"
